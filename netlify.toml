# Netlify configuration for Next.js with App Router in src/

[build]
base = "."              # Root of the repository
command = "npm run build"
publish = ".next"       # Next.js default build output

[build.environment]
NODE_VERSION = "20"
NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
package = "@netlify/plugin-nextjs"








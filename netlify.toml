# Netlify configuration for Next.js (App Router in src/)

[build]
command = "npm run build"
publish = ".next"        # Next.js default build output
# No base directory — the plugin will detect src/app automatically

[build.environment]
NODE_VERSION = "22"      # match your local Node version
NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
package = "@netlify/plugin-nextjs"





